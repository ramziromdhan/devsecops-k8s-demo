# .checkov.yaml
# Checks ignorés avec justification pour le projet devsecops-k8s-demo

skip-check:
  - CKV_K8S_21   # default namespace — projet de démo, namespace dédié en prod
  - CKV_K8S_40   # UID > 10000 — on utilise 1001, suffisant pour éviter conflits host
  - CKV_K8S_15   # pullPolicy Always — intentionnellement Never pour Minikube local
  - CKV_K8S_14   # image tag fixed — tag = SHA commit GitHub, reproductible
  - CKV_K8S_43   # image digest — tag SHA commit suffit pour ce projet
  - CKV_K8S_35   # secrets as files — démo, en prod Vault ou External Secrets Operator