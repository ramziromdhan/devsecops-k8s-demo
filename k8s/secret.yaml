apiVersion: v1
kind: Secret
metadata:
  name: linsoft-demo-secret
  namespace: default
type: Opaque
data:
  # DEMO ONLY — base64("secret_key")
  # En production : utiliser Sealed Secrets, Vault, ou External Secrets Operator
  # Ne jamais commiter de vrais secrets dans un manifest Kubernetes
  API_KEY: "c2VjcmV0X2tleQ=="  # base64 encoded value of "secret_key
